{
  "name": "uthm",
  "version": "1.0.0",
  "description": "Student Course Registration System",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@sap/cds": "^9",
    "express": "^4",
    "@cap-js/sqlite": "^2"
  },
  "scripts": {
    "start": "cds-deploy --to sqlite && cds-serve"
  },
  "private": true
}