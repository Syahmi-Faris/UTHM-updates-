{
  "name": "uthm",
  "version": "1.0.0",
  "description": "Student Course Registration System",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4",
    "@cap-js/postgres": "^1"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2"
  },
  "scripts": {
    "start": "cds-serve",
    "build": "cds build --production"
  },
  "private": true
}