{
  "name": "uthm",
  "version": "1.0.0",
  "description": "Student Course Registration System",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds": "^9",
    "express": "^4"
  },
  "scripts": {
    "deploy": ".\\node_modules\\.bin\\cds deploy --to sqlite:db.sqlite",
    "dev": ".\\node_modules\\.bin\\cds watch",
    "start": "npx --no @sap/cds-dk deploy --to sqlite:db.sqlite && npx --no @sap/cds-dk serve"
  },
  "private": true,
  "devDependencies": {
    "@sap/cds-dk": "^9.8.3"
  }
}
